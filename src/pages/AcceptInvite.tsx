import { AcceptInviteForm } from "../components/forms";

export const AcceptInvite = () => {
  throw Error("thesting")
  return (
    <div className="min-h-screen bg-gradient-to-br from-primary-secondary/20 to-primary-tertiary/30">
      <AcceptInviteForm />
    </div>
  );
};
